# values-alerting-node.yaml
grafana:
  alerting:
    node-rules.yaml:
      apiVersion: 1
      groups:
        # --- GROUP 1: PROD SG ---
        - orgId: 1
          name: "node-resources-prod-sg"
          folder: "Cluster: Prod SG"
          interval: 1m
          rules:
            - uid: "high_cpu_node_prod_sg"
              title: "High CPU Usage (Prod SG)"
              condition: "C"
              for: 5m
              labels:
                severity: "critical"
                team: "devops"
                env: "prod-sg"
              annotations:
                summary: 'High CPU on {{ "{{" }} $labels.instance {{ "}}" }}'
                description: 'Node {{ "{{" }} $labels.instance {{ "}}" }} at {{ "{{" }} $values.B.Value | humanize {{ "}}" }}%'
              data:
                - refId: "A"
                  datasourceUid: "prom-prod-sg"
                  queryType: "range"
                  relativeTimeRange: { from: 600, to: 0 }
                  model:
                    expr: "100 - (avg by (instance) (rate(node_cpu_seconds_total{mode='idle'}[5m])) * 100)"
                    refId: "A"
                - refId: "B"
                  datasourceUid: "__expr__"
                  queryType: "reduce"
                  model: { expression: "A", reducer: "mean", refId: "B", type: "reduce" }
                - refId: "C"
                  datasourceUid: "__expr__"
                  queryType: "math"
                  model: { expression: "$B > 85", refId: "C", type: "math" }

        # --- GROUP 2: NON-PROD SG ---
        - orgId: 1
          name: "node-resources-nonprod-sg"
          folder: "Cluster: NonProd SG"
          interval: 1m
          rules:
            - uid: "high_cpu_node_nonprod_sg"
              title: "High CPU Usage (NonProd SG)"
              condition: "C"
              for: 5m
              labels:
                severity: "critical"
                team: "devops"
                env: "nonprod-sg"
              annotations:
                summary: 'High CPU on {{ "{{" }} $labels.instance {{ "}}" }}'
                description: 'Node {{ "{{" }} $labels.instance {{ "}}" }} at {{ "{{" }} $values.B.Value | humanize {{ "}}" }}%'
              data:
                - refId: "A"
                  datasourceUid: "prom-nonprod-sg"
                  queryType: "range"
                  relativeTimeRange: { from: 600, to: 0 }
                  model:
                    expr: "100 - (avg by (instance) (rate(node_cpu_seconds_total{mode='idle'}[5m])) * 100)"
                    refId: "A"
                - refId: "B"
                  datasourceUid: "__expr__"
                  queryType: "reduce"
                  model: { expression: "A", reducer: "mean", refId: "B", type: "reduce" }
                - refId: "C"
                  datasourceUid: "__expr__"
                  queryType: "math"
                  model: { expression: "$B > 85", refId: "C", type: "math" }

        # --- GROUP 3: PROD AU ---
        - orgId: 1
          name: "node-resources-prod-au"
          folder: "Cluster: Prod AU"
          interval: 1m
          rules:
            - uid: "high_cpu_node_prod_au"
              title: "High CPU Usage (Prod AU)"
              condition: "C"
              for: 5m
              labels:
                severity: "critical"
                team: "devops"
                env: "prod-au"
              annotations:
                summary: 'High CPU on {{ "{{" }} $labels.instance {{ "}}" }}'
                description: 'Node {{ "{{" }} $labels.instance {{ "}}" }} at {{ "{{" }} $values.B.Value | humanize {{ "}}" }}%'
              data:
                - refId: "A"
                  datasourceUid: "prom-prod-au"
                  queryType: "range"
                  relativeTimeRange: { from: 600, to: 0 }
                  model:
                    expr: "100 - (avg by (instance) (rate(node_cpu_seconds_total{mode='idle'}[5m])) * 100)"
                    refId: "A"
                - refId: "B"
                  datasourceUid: "__expr__"
                  queryType: "reduce"
                  model: { expression: "A", reducer: "mean", refId: "B", type: "reduce" }
                - refId: "C"
                  datasourceUid: "__expr__"
                  queryType: "math"
                  model: { expression: "$B > 85", refId: "C", type: "math" }